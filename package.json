{
  "name": "submit-dir",
  "version": "1.0.0",
  "description": "CLI tool for submitting URLs to aidirs.org",
  "main": "dist/index.js",
  "bin": {
    "submit-dir": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "README.zh-CN.md",
    "install.sh"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "keywords": [
    "aidirs",
    "cli"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.0",
    "fs-extra": "^11.2.0",
    "inquirer": "^9.2.20"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/inquirer": "^9.0.9",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
